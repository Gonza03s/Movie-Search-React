import React from 'react'
import ReactDOM from 'react-dom/client'
import './styles.css/movieSearch.css'
import {MovieSearch} from './MovieSearch'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MovieSearch/>
  </React.StrictMode>,
)
